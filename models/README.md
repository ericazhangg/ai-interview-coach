# Models

This project does not train a custom local model from scratch, so this folder is used for model documentation instead of saved weights.

Current contents include:

- `model_config.json`
  - records the main models and services used in the project
- `model_notes.md`
  - explains the pretrained and API-based models used for scoring and transcription
